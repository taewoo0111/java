package test.main;
/*
 *  heap=객체가 만들어짐  stack=지역변수가 만들어짐  static=클래스가 올라가는 영역
 */

import test.mypac.MyUtil;
import test.mypac.YourUtil;

public class MainClass07 {
	public static void main(String[] args) {	
		// MyUtil 클래스의 static 메소드 호출
		MyUtil.send();
		//MyUtil 클래스의 static 필드 참조
		String a = MyUtil.version;
		System.out.println(a);
		
		YourUtil.remove();
		String b = YourUtil.color;
		// class System
		//	public static PrintStream out = ??? 이런 구성일 것이다.
	}
}
